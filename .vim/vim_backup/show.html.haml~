.container.top-container.last-container
  .row
    .span8.offset2.result-window
      = render "user/users/tabs"
      .row.page-title-h4
        .span5
          %h4 請求先情報
        .span3.al-r
          - if @user_profile.present?
            = link_to "編集", edit_user_user_profile, class: "btn btn-primary"
      - if @user_profile.present?
        %table.table.table-bordered
          %tr
            %th.span3 住所
            %td= @user_profile.address
          %tr
            %th.span3 会社名
            %td= @user_profile.company
          %tr
            %th.span3 氏名
            %td= @user_profile.name
      - else
        %h5 登録されていません
        = link_to "登録する", new_user_user_profiles_path, class: "btn btn-primary"
