%tr
  %td= "#{datetime_str(listing_pre_booking.start_at)}"
  %td= "#{listing_pre_booking.hours} 時間"
  %td= "#{listing_pre_booking.party} 人"
  %td= listing_pre_booking.name
  %td= "#{listing_pre_booking.price} 円"
  %td= datetime_str(listing_pre_booking.created_at)
  %td
    %p= t(listing_pre_booking.status_str, scope: "values.pre_booking.status")
    %p= link_to "詳細", provider_pre_booking_path(listing_pre_booking)
    -if listing_pre_booking.pending?
      = render 'provider/pre_bookings/approve_button', pre_booking: listing_pre_booking
      = render 'provider/pre_bookings/decline_button', pre_booking: listing_pre_booking
    %p= link_to "メッセージを送信する", provider_pre_booking_messages_path(pre_booking), method: :patch
