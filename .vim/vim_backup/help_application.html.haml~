!!!
%html
  %head
    = stylesheet_link_tag "pc_app", media: "all", "data-turbolinks-track" => true
    = render "partials/provider_js"
    = render "partials/favicon"
    = csrf_meta_tags
    = display_meta_tags
    = yield :addon_scripts
  %body{class: body_css_class}
    - if current_provider_user
      = render "layouts/provider/nav_tab"
    - else
      .header= render "layouts/provider/header"
    .container.top-container.last-container.provider-help-app
      .row
        .span3.help-nav
          = render 'provider/helps/nav_help'
        .span9
          = yield
    .footer= render "partials/footer"

    = render "partials/analytics"
    = render "partials/mixpanel_provider"

  != render_castro
