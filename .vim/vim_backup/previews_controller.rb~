class Provider::PreviewsController < ProviderApplicationController
  before_action :set_listing

  def show
  end

  def price_plans
    @price_plans = @listing.price_plans.includes(:price_plan_timetables)
  end

  private

  def set_listing
    @listing = current_provider.listings.find params[:listing_id]
  end

end
