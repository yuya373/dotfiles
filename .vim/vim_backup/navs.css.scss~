ul.menu-justified {
  list-style: none;
  margin: 0;
  li {
    float: left;
    a {
      display: block;
      width: 100%;
      text-align: center;
    }
  }
}

.menu-justified-w4 {
  li {
    width: 25%;
  }
}

ul.menu-justified-hiw {
  a {
    padding: 15px 0;
    font-weight: bold;
    color: white;
  }
}

ul.pre-header-links {
  padding-right:10px;
  margin: 0;
  li a {
    color: white;
    &:hover {
      // color: white;
      text-decoration: underline;
    }
  }

  li.line {
    color : lighten($brandSemiDark, 15%);
    color: rgba(white, 0.5);
  }
}
