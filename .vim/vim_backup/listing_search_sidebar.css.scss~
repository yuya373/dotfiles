
$ls-sidebar-width: get_span_width(3, $gridColumnWidth, $gridGutterWidth);
$ls-inner-padding-x: 10px;
$ls-inner-padding-y: 10px;
$sb-ls-font-size: 13px;
$ls-row-width: $ls-sidebar-width - $ls-inner-padding-x * 2;


.ls-sidebar-legend {
	margin-top: 10px;

	h6 {
		line-height: 31px;
		margin: 0;
		padding: 0;
		font-size: $baseFontSize;
	}
	border-bottom: 1px dashed #ddd;
}

.ls-sidebar {
	margin-top: 10px;
//  background: $grayLighter;
	background: #f6f6f6;
  @include border-radius($borderRadiusLarge);
	width: $ls-sidebar-width;
	font-size: $sb-ls-font-size;
}

// .ls-sidebar-header {
// 	padding: $ls-inner-padding-y $ls-inner-padding-x;
// 	h4 {
// 		margin: 0;
// 		padding: 0;
// 		font-size: $baseFontSize;
// 	}
// }

.ls-sidebar-body {
	padding: $ls-inner-padding-y 0;
	margin: 0 $ls-inner-padding-x;
}


.ls-sidebar-form {
	margin: 0;

	.ls-row {
		margin-bottom: 10px;
	}

	label {
		font-size: $sb-ls-font-size;
		color: $gray;
		font-weight: bold;
	}

	.ls-action-row {
		margin-top: 10px;
		text-align: right;
	}
}


form.ls-sidebar-form {
	input {
		font-size: 13px;
	}

	input.input-time  { width: 80px; }
	input.input-hours { width: 30px; }
	select.ls-neighborhood-input {
		$_h: 34px;
		line-height: $_h;
		height: $_h;
		margin: 0;
		color: white;
		width: $ls-row-width;
	}

	.select2-search-field {
		input {
			$_w: $ls-row-width - 5px * 2;  // 5px select2's x padding
			width: $_w;
			min-width: $_w;

		}
	}
}

.serch-query{
  table{
    text-align: center;
    th{
      width: 85px;
    }
  }
}

