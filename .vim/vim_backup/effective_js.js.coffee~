arrayLike = {
  0: "a"
  1: "b"
  2: "c"
  length: 3
}

result = [].map.call(arrayLike, (s) ->
  s.toUpperCase()
)

# console.log arrayLike
# console.log result
# console.log arrayLike[0]
# console.log [].map.call("abc", (s) ->
  # s.toUpperCase()
# )

ary = [].slice.call(arrayLike)


aaa = do ->
  ["names"].concat(ary)

console.log aaa
