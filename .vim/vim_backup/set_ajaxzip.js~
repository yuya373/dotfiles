// function zenToHan (string) {
  // var str = '';
  // var that = string
  // function toHan(match){
    // str += String.fromCharCode(match.charCodeAt(0) - 0xFEE0)
  // }
  // if (that.match(/[0-9]/g)) {
    // return that.toString();
  // } else {
    // that.replace(/[０-９]/g, toHan)
    // console.log(str)
    // return str
  // }
// }

// var zip = document.getElementById('listing_listing_location_attributes_zip_code_before')

// console.log(zip.value)

// zip.onchange = alert('hello');
// zenToHan(zip.value);

var zip_code = document.getElementById('listing_listing_location_attributes_zip_code_after')

zip_code.onchange = AjaxZip3.zip2addr('listing[listing_location_attributes][zip_code_before]','listing[listing_location_attributes][zip_code_after]','listing[listing_location_attributes][prefecture]','listing[listing_location_attributes][city]', 'listing[listing_location_attributes][lot_number]');
