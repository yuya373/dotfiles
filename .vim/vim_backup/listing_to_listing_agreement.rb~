Listing.all.each do |listing|
  agreement = listing.agreement
  listing_id = listing.id
  if agreement.present?
    la = ListingAgreement.new
    la.listing_id = listing_id
    la.content = agreement
    la.save!
  end
end

