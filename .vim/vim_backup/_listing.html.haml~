%tr
  %td
    = image_tag listing.thumb, width: 50, height: 50
    = link_to listing.title, provider_listing_path(listing), class: "strong tx1-2"
  %td= listing_status_label(listing)
  %td
    - if listing.setup_done?
      %p= link_to("カレンダー編集",
      scheduler_provider_listing_room_calendars_path(listing))
      -# = link_to("営業日設定",
      -# edit_all_provider_listing_calendar_configurations_path(listing))
      %p= link_to "スペース情報編集", edit_provider_listing_path(listing)
      %p= link_to "自社予約一覧", provider_listing_alien_bookings_path(listing)
      - if listing.published?
      %p= link_to "公開ページ", listing_path(listing)
    - else
      %p= link_to "再開する", new_provider_listing_price_plan_setup_path(listing)
    %td
      - if listing.reviews.present?
        = link_to("#{listing.reviews.count}件", provider_listing_reviews_path(listing))
      - else
        なし
