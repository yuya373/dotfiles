/*
 * TestComponent.js
 */

define(["core/component"], function(component) {
  return component.extend({
    install: function() {
      console.log("TestComponent - installed");
      $(this.el).html("TestComponent installed");
      $(this.el).bind("click" function () {
        console.log( "clicked" )
      })
    }
  });
});
