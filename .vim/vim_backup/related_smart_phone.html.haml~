.group-header.group-header-brand.group-header-default
  %h1 #{@head}のレンタルスペース・貸し会議室

= render "partials/neighborhood/listing_toolbar", count: @listings.size

.list-group
  %ul.list-cells.tall-cells
    - @listings.each do |listing|
      %li= render 'partials/listing_cell', listing: listing

- if @listings.size > 2
  = render "partials/neighborhood/listing_toolbar", count: @listings.size

= render "partials/neighborhood/select_neighborhoods", current: @neighborhood

= render "partials/listing_search/search_modal"
