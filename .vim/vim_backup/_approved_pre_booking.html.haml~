%tr
  %td= link_to pre_booking.listing.title.truncate(15, separator: /\s/), listing_path(pre_booking.listing)
  %td= pre_booking.start_at_str
  %td= "#{ pre_booking.hours } 時間"
  %td= "#{ pre_booking.party } 人"
  %td= pre_booking.name
  %td= pre_booking.created_at_str
  %td
    %p
      = link_to "詳細", provider_pre_booking_path(pre_booking)
    %p
      - if pre_booking.pending?
        = render 'provider/pre_bookings/approve_button', pre_booking: pre_booking
        = render 'provider/pre_bookings/decline_button', pre_booking: pre_booking
    - if pre_booking.approved? || pre_booking.declined?
      %p
        = link_to "メッセージを送信", new_provider_pre_booking_messages_path(pre_booking)

