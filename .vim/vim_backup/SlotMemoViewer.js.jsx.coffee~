###* @jsx React.DOM ###

define(["react"], (React) ->
  SlotMemoViewer = React.createClass {
    getInitialState: ->
      {
        "hidden"    : true
        "style"     : {}
        "start_at"  : null
        "end_at"    : null
        "date"      : null
      }

    show: (params)->
      @.setState {
        "hidden"   : false
        "start_at" : params.start_at
        "end_at"   : params.end_at
        "date"     : params.date
      }
      console.log "ShowMemo#show"
      console.log @

    render: ->
      console.log "SlotMemoViewer rendered"
      # if this.state.hidden == true then return `<div></div>`

      contentStyle = {"display":"block"}

      return `(
        <div className="slot-prompt-shell" style={this.state.style} >
            <div className="slot-price-viewer mod mod-default mod-round" style={contentStyle} >
              <div className="viewer-header">
                料金
              </div>
              <div className="inner-mod">
                {this.renderContent()}
              </div>
            </div>
          </div>
      )`

    renderContent: ->
      return `(
      <table className="table table-bordered table-fit table-small">
        <tr>
          <th> test </th>
          <td> test </td>
        </tr>
      </table>
      )`

  }
  return SlotMemoViewer
)
