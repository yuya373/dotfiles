- set_listing_availability_meta_tags(@listing)
.group-header.group-header-default.group-header-light
  = render "partials/flashes"
  %h1.tx-truncated 空室確認: #{@listing.title}

= form_tag @listing.link_to_apply,
  {class: "form-horizontal-striped", id: "lsAvForm", method: :get} do
  = render 'partials/listing_search/query_form', listing: @listing
  .control-group
    .container.last-container
      - if params[:unavailable] == "true"
        = link_to "空室カレンダーを見る",
        calendar_listing_path(@listing, keeped_query),
        class: "btn btn-primary btn-block btn-large last-container"
        - if params[:unknown] == "true"
          = link_to "空室状況を問い合わせる",
          new_listing_booking_request_path(@listing, keeped_query),
          class: "btn btn-success btn-block btn-large"
          %p
            %small 入力した日時の空室状況をワンクリックでお問い合わせできます。
      - else
        %input#submit.btn.btn-block.btn-large.btn-success{:type=>"submit", :value=>"予約へ進む"}

- $castro.add_js_component("lsAvForm", "LsAvailabilityForm",
{}, {"nextUrl" => @js_params[:prebookingUri]});
