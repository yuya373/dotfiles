
.container.top-container.last-container
  .row
    .span10.offset1
      = render 'tab_header'

      .page-header-single-pane
        %h1
          %i.icon-envelope
            送信済みメッセージ
          %small
            = "#{@conversations.to_a.count}件"
      - if @conversations.present?
        .mod-group
          .mod
            .inner-mod
              %table.table.table-bordered.messages
                %thead
                  %tr
                    %th.date 日時
                    %th 件名
                    %th.people 受信者
                %tbody
                  = render partial: 'conversation', collection: @conversations
      - else
        %h4 メッセージはありません
