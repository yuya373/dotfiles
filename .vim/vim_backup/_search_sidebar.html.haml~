.ls-sidebar-legend
  %h6
    %i.icon-caret-down.tx-pink
    条件で絞り込む

.ls-sidebar
  .ls-sidebar-body
    = form_tag search_listings_path, class: "ls-sidebar-form", id: "sbSearch",
      method: :get do
      = hidden_field_tag :place, request.path
      = hidden_field_tag :usage_id, keeped_query[:usage_id]
      = hidden_field_tag :prefecture, keeped_query[:prefecture]
      = hidden_field_tag :station, keeped_query[:station]
      = hidden_field_tag :price_min, keeped_query[:price_min]
      = hidden_field_tag :price_max, keeped_query[:price_max]
      = hidden_field_tag :category, keeped_query[:category]
      = hidden_field_tag :party_min, keeped_query[:party_min]
      = hidden_field_tag :party_max, keeped_query[:party_max]
      .ls-row.rel
        %label ご利用日
        = text_field_tag :date, default_date_str(keeped_query[:date]), {placeholder: "ご利用日", class: "for-picker input-block-level"}

      .ls-row
        %label ご利用時間
        = text_field_tag :time, keeped_query[:time], {class: "input-time for-picker", readonly: "true", placeholder: "ご利用時刻"}
        %span.small から
        %input#timeValue{type: "hidden", name: "time", value: keeped_query[:time]}
        = number_field_tag :hours, keeped_query[:hours], class: "input-hours"
        %span.small 時間
      .ls-row
        %label ご利用人数
        .input-append
          = number_field_tag :party, keeped_query[:party], class: "span1"
          %span.add-on 人
      .ls-row
        %label 地域
        = select_tag :neighborhood, neighborhood_options_for_select,
          class: "ls-neighborhood-input for-multiple",
          multiple: true, placeholder: "地域を選択...", prompt: "都内全域"
      .ls-row.ls-action-row
        %input#submit.btn.btn-primary.btn-small{type: "submit", value: "検索"}

-#- if keeped_query.present?
-#  = render 'partials/listing_search/show_keeped_query'

- $castro.add_js_component("sbSearch", "SidebarLsForm", {}, {nextUrl: search_listings_path});
