class Admin::EquipmentsController < AdminApplicationController

  def index
    @eqs = Equipment.all
    @equipment = Equipment.new
  end

  def create
    @equipment = Equipment.new(eq_params)
    if @equipment.save
      redirect_to admin_equipments_path
    else
      redirect_to 
    end
  end

  private

  def set_eq
  end

  def eq_params
    params.require(:equipment).permit(:name)
  end
end
